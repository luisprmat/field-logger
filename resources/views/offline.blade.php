<x-layout>
    <div class="prose mx-auto p-6 text-center">
        <h1 class="mb-4 text-lg font-semibold">{{ __("You're Offline") }}</h1>
        <p class="text-white/70">
            {{ __("We can't reach the network right now. Try again when you're back online.") }}
        </p>
    </div>
</x-layout>
